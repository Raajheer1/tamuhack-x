import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.raajpatel.tamuhackx',
  appName: 'aase',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
